
module testservant
{

struct Send
{
  0 optional int a = 0;
	1 optional string b = "";
	2 optional long c = 0;
	3 optional bool d = true;
};

interface TestServant
{
	int testHello(string s, int i, long l, bool b, byte c, vector<byte> vt, map<string, string> mp, Send send, out string r);
	
	void testVoid(string s, int i, long l, bool b, byte c, vector<byte> vt, map<string, string> mp, Send send, out string r);
};

}; 
